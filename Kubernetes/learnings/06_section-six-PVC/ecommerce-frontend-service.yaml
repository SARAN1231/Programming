apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend-service
  namespace: ecommerce
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: ecommerce-frontend-app
  ports:
    - port: 80
      targetPort: 80
