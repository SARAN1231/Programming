apiVersion: v1
kind: Service
metadata:
  name: ecommerce-backend-service
spec:
  selector:
    app.kubernetes.io/instance: ecommerce-backend-app
  ports:
  - port: 8080
    targetPort: 8080
